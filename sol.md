出题人题解。

## 分析

任意一条合法路径都可拆分为三个阶段：

1. 前缀：从 $n$ 出发，总边权和 $\le t$ 的一段路径；
2. 跨越：走过某条边，使总权首次超过 $t$，若整条路径总权 $\le t$，则跨越边就是最后一步到 $1$ 的边，此时后缀为空；
3. 后缀：从跨越边的终点出发，此时无论怎么走都会使路径总权变大，所以直接沿一条不折返的最短路径直走到 $1$ 即可。

于是可以得出核心思路：先预处理后缀部分，接着在处理前缀的时候考虑每一条边 $(u, v)$ ，假设这条边就是跨越边，然后运用已知的信息拼凑出答案并更新。

## 第一步：处理后缀

以 $1$ 为源点用 Dijkstra 预处理每个节点 $u$ 到 $1$ 的合法的首边不同的最短路与次短路就行了，分别用 $dis[0/1][u]$ 表示。同时用 $nxt[u]$ 记录为实现最短路时从 $u$ 走出的第一条边的编号。

处理的时候要记得路径不能立即折返。

## 第二步：处理前缀

可以运用动态规划的思想，以当前总权 $w$ 作为阶段，进行分层 BFS。

定义状态 $(w, e_{in})$ 表示：从 $n$ 出发，当前总边权和为 $w$，最后一步是通过有向边 $e_{in}$ 进入当前节点。\
定义数组 $vis[w][e]$ 表示： 状态 $(w, e)$ 是否已生成。

最开始我们需要添加一条虚拟边 $E$，边权为 $0$ 且指向 $n$，然后将状态 $(0, E)$ 标记为已生成。

接着按 $w = 0,1,\dots,t$ 逐层处理。对于每一层 $w$：

1. 此时搜索队列为空。先扫描一遍 $vis[w]$ 收集已生成的状态，把满足 $vis[w][e] = 1$ 的边 $e$ 加入搜索队列中。
2. 对于每个状态 $(w, e_{in})$ 指向的节点 $u$，枚举它的出边 $e_{out}$ 和该边指向的点 $v$，得到新权值 $w' = w + W_{e_{out}}$，其中 $W_{e_{out}}$ 表示当前出边 $e_{out}$ 的边权。此时假设 $e_{out}$ 就是跨越边，立即尝试用 $w'$ 和我们之前预处理的后缀进行拼接并更新答案 $ans$：优先使用 $dis[0][v]$（要求存在且 $nxt[v] \ne e_{out}^{-1}$，其中 $e_{out}^{-1}$ 为 $e_{out}$ 的反向边），否则使用 $dis[1][v]$（如果存在）。如果当前答案更优，则更新答案。
3. 若 $w' \le t$ 且状态 $(w', e_{out})$ 未生成，则将该状态标记为已生成。特别地，如果该状态未生成且满足 $w' = w$，说明它还在这个层次中，应该立即入队。
4. 重复处理步骤 2 ~ 3 直到队列为空。

## 优化

### 剪枝优化

回顾分层 BFS 的部分，我们发现：单层每个节点 $u$ 可能被 $deg(u)$ 条不同的入边到达（$deg(u)$ 为 $u$ 的度数），而每条入边都会遍历全部 $deg(u)$ 条出边，因此节点 $u$ 单层的出边访问次数可达 $deg^2(u)$ 条。这便是导致算法 TLE 的问题所在。

为了解决这一问题，我们需要定义数组 $cnt[w][u]$ 表示：记录节点 $u$ 在权值 $w$ 层已经接收的不同的入边个数。限制节点 $u$ 在权值 $w$ 层最多只能被两条入边访问，从而使节点 $u$ 单层的出边访问时间复杂度控制在 $O(deg(u))$ 以内，于是把单层处理的时间复杂度控制在 $O(m)$ 以内。故剪枝优化后的时间复杂度为 $O(mt)$。

为什么只保留两条边？因为任意入边的作用仅仅在于决定使用 $dis[0][v]$ 还是 $dis[1][v]$ 作为后缀，因此能够产生的答案最多就两种，所以我们只需保留一条与 $nxt[v]$ 不冲突的入边和一条与 $nxt[v]$ 冲突的入边，就覆盖了所有可能的总权值。第三条入边必然重复其中一种情况，可以安全剪掉。

### 空间优化

不难发现 $cnt$ 数组的空间复杂度是 $O(nt)$ 的。注意到题目中只给出了 $mt$ 的限制但是没有给出 $nt$ 的限制，所以我们只需要关心和 $1$ 连通的点（显然也会和 $n$ 连通）就行了，其它不连通的点没有必要考虑。于是可以在算法开始之前跑一遍 BFS 求出和 $1$ 连通的点，只给它们分配内存。这样子的话空间复杂度会变成 $O(mt)$。

---

最终时间复杂度为 $O(m \log n + mt)$ 且常数较小，空间复杂度为 $O(mt)$。