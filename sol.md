出题人题解。

友情链接（如果你觉得本题解比较复杂的话不妨看看这个）：[验题人题解](https://www.luogu.com.cn/article/lim3luqq)。

## 题目大意

给定一个 $n$ 个节点、$m$ 条边的无向图（无重边和自环），每条边有非负整数权值 $w$。起点为 $n$，终点为 $1$，要求找一条路径满足：

- 不能连续经过同一条边（不能立即折返）；
- 节点 $1$ 仅能作为终点出现，且恰好在路径末尾出现一次；
- 路径可以重复访问其他节点。

设路径总边权和为 $W$，给定目标时间 $t$，求 $\min |W - t|$。

## 分析

任意一条合法路径都可拆分为三个阶段：

1. 前缀：从 $n$ 出发，总边权和 $\le t$ 的一段路径；
2. 跨越：走过某条边，使总权首次超过 $t$，若整条路径总权 $\le t$，则跨越边就是最后一步到 $1$ 的边，此时后缀为空；
3. 后缀：从跨越边的终点出发，此时无论怎么走都会使路径总权变大，根据贪心，直接沿一条不折返的最短路径直走到 $1$ 即可。

于是可以得出核心思路：考虑每一条边 $(u, v)$ ，假设这条边就是跨越边，然后运用已知的信息拼凑出答案。

对于一条边 $(u, v)$ 的后缀部分，我们显然可以在 $O(n)$ 预处理过后以 $O(1)$ 的查询得到这一部分的答案。那前缀部分可不可以使用同样的思路呢？这其实做不到，因为从起点 $n$ 走到 $u$ 的过程中，走过的总权值 $w$ 并不是固定的，它可以是 $0 \sim t$ 中的任意一个值。而且最后一步是由哪条边进入 $u$ 的，会影响后续能否使用某条后缀的首边，这同时也启发我们：处理后缀的时候应该对每个点求出一条最短路和次短路（二者允许相等，但是首边不能相同，具体解释见下文）。

综上所述，我们不能简单地枚举每一条边，而是在处理前缀的时候一并更新答案。

## 第一步：处理后缀

后缀很好处理，直接以 $1$ 为源点用 Dijkstra 预处理每个节点到 $1$ 的合法的首边不同的最短路与次短路就行了，具体需要记录以下信息：

定义 $dis[0][u]$ 为从 $u$ 出发到 $1$ 的最短距离，$nxtEdge[u]$ 为实现该最短路时从 $u$ 走出的第一条边的编号；\
定义 $dis[1][u]$ 为从 $u$ 出发到 $1$ 的、第一条边与 $nxtEdge[u]$ 不同的最短距离。

处理的时候要记得路径不能立即折返。

## 第二步：处理前缀

可以运用动态规划的思想，以当前总权 $w$ 作为阶段，进行分层 BFS。

在开始之前，我们需要定义以下内容：

定义状态 $(w, e_{in})$ 表示：从 $n$ 出发，当前总边权和为 $w$，最后一步是通过有向边 $e_{in}$ 进入当前节点。\
定义数组 $vis[w][e]$ 表示： 状态 $(w, e)$ 是否已生成。

最开始我们需要添加一条虚拟边 $E$，边权为 $0$ 且指向 $n$，然后将状态 $(0, E)$ 标记为已生成。

接着按 $w = 0,1,\dots,t$ 逐层处理。对于每一层 $w$：

1. 此时搜索队列为空。先扫描一遍 $vis[w]$ 收集已生成的状态，把满足 $vis[w][e] = 1$ 的边 $e$ 加入搜索队列中。
2. 对于每个状态 $(w, e_{in})$ 指向的节点 $u$，枚举它的出边 $e_{out}$ 和该边指向的点 $v$，得到新权值 $w' = w + W_{e_{out}}$，其中 $W_{e_{out}}$ 表示当前出边 $e_{out}$ 的边权。此时假设 $e_{out}$ 就是跨越边，立即尝试用 $w'$ 和我们之前预处理的后缀进行拼接并更新答案 $ans$：优先使用 $dis[0][v]$（要求存在且 $nxtEdge[v] \ne e_{out}^{-1}$，其中 $e_{out}^{-1}$ 为 $e_{out}$ 的反向边），否则使用 $dis[1][v]$（如果存在）。如果当前答案更优，则更新答案。
3. 若 $w' \le t$ 且状态 $(w', e_{out})$ 未生成，则将该状态标记为已生成。特别地，如果该状态未生成且满足 $w' = w$，说明它还在这个层次中，应该立即入队。
4. 重复处理步骤 2 ~ 3 直到队列为空。

## 优化

### 剪枝优化

回顾分层 BFS 的部分，我们发现：单层每个节点 $u$ 可能被 $deg(u)$ 条不同的入边到达（$deg(u)$ 为 $u$ 的度数），而每条入边都会遍历全部 $deg(u)$ 条出边，因此节点 $u$ 单层的出边访问次数可达 $deg^2(u)$ 条。这便是导致算法 TLE 的问题所在。

为了解决这一问题，我们需要定义数组 $inCnt[w][u]$ 表示：记录节点 $u$ 在权值 $w$ 层已经接收的不同的入边个数。限制节点 $u$ 在权值 $w$ 层最多只能被两条入边访问，从而使节点 $u$ 单层的出边访问时间复杂度控制在 $O(deg(u))$ 以内，于是把单层处理的时间复杂度控制在 $O(m)$ 以内。故剪枝优化后的时间复杂度为 $O(mt)$。

为什么只保留两条边？因为任意入边的作用仅仅在于决定使用 $dis[0][v]$ 还是 $dis[1][v]$ 作为后缀，因此能够产生的答案最多就两种，所以我们只需保留一条与 $nxtEdge[v]$ 不冲突的入边和一条与 $nxtEdge[v]$ 冲突的入边，就覆盖了所有可能的总权值。第三条入边必然重复其中一种情况，可以安全剪掉。

### 空间优化

不难发现，$inCnt$ 数组的空间复杂度是 $O(nt)$ 的，非常危险，所以我们需要对它进行空间优化。

这里给出两种正确的方案，任选其一即可：

#### 优化空间复杂度

注意到题目中只给出了 $mt$ 的限制但是没有给出 $nt$ 的限制，这启发我们需要想办法把它的空间复杂度优化成 $O(mt)$。

我们只需要关心和 $1$ 连通的点（显然也会和 $n$ 连通）就行了，其它不连通的点没有必要考虑。所以可以在算法开始之前跑一遍 BFS 求出和 $1$ 连通的点，只给它们分配内存。

#### 卡常：二进制压缩

注意到我们限制了节点 $u$ 在权值 $w$ 层最多只能被两条入边访问，因此 $inCnt$ 数组每个单元只需存储 $\{0, 1, 2\}$，故分配 $2$ 位的空间就行了。

考虑使用二进制压缩。这里以 `unsigned long long` 为例，给每个单元开 $2$ 位的空间进行存储，一个 `unsigned long long` 可以存放 $32$ 个单元。

不管是增加计数还是读取计数，我们都需要先计算操作目标在哪个位置：数组编号 $idx = \lfloor \frac{u}{32} \rfloor$，位偏移 $shift = (u \bmod 32) \times 2$。

然后就可以运用位运算的知识进行修改和读取了，具体请看代码：

::::info[这一部分的代码]
```cpp
void addCnt(int w, int u) {
	int idx = u >> 5, shift = (u & 31) << 1; ull val = (inCnt[w][idx] >> shift) & 3;
	if (val < 2) {
		val++;
		inCnt[w][idx] = (inCnt[w][idx] & ~(3ULL << shift)) | (val << shift);
	}
}
int qryCnt(int w, int u) {
	int idx = u >> 5, shift = (u & 31) << 1;
	return (inCnt[w][idx] >> shift) & 3;
}
```
::::

虽然空间复杂度没有变化，但是能够大幅度节省内存。